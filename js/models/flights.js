/*
 * Main flight model code
 */

define([
  'underscore',
  'backbone'
], function(_, Backbone){

	var Flight = Backbone.Model.extend({
    	defaults: {}, // No defaults for now
    	

  	});

  	return Flight;
});